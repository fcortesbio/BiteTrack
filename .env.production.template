# BiteTrack Production Environment Configuration Template
# Copy this to .env.production and update with your actual values

NODE_ENV=production
PORT=3000

# MongoDB Configuration - UPDATE WITH YOUR PRODUCTION VALUES
# Connection string format (for API)
MONGO_URI=mongodb://admin:your-production-password@mongo-host:27017/bitetrack

# Separate credentials (for scripts and docker-compose)
# These should match the credentials in MONGO_URI above
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-production-password

# JWT Configuration - CHANGE THESE IN PRODUCTION!
JWT_SECRET=your-super-secure-production-jwt-secret-here
JWT_EXPIRES_IN=7d

# CORS Configuration - Production Frontend URLs
# Add your production frontend domain(s) here (comma-separated)
# Examples:
# FRONTEND_URLS=https://yourdomain.com,https://app.yourdomain.com
# FRONTEND_URLS=https://myrestaurant.com,https://admin.myrestaurant.com
FRONTEND_URLS=https://yourdomain.com

# Production Settings
DEBUG=false
LOG_LEVEL=info

# Security Settings (Optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100